import { defineConfig } from 'vite'

// https://vitejs.dev/config/
export default defineConfig({
    // No special plugins or options are needed for Lightweight Charts to work.
    // Vite handles resolving the 'lightweight-charts' import from node_modules.
})
